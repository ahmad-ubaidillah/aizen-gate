# User Guide

## Overview
This application is managed by Aizen-Gate.

## Getting Started
1. Clone the repository
2. Run `npx aizen-gate start` to initialize context
3. Run `npx aizen-gate auto` to implement features
